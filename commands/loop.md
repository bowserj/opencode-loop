---
description: Start a Bybrawe loop job. Usage: /loop 0s <prompt>, /loop 5m <prompt>, /loop 200m /compact, /loop 10m !npm test
argument-hint: "INTERVAL [--name NAME] [--max-runs N] [--no-now] ACTION"
---
# Bybrawe Loop

Loop request:

```text
$ARGUMENTS
```

The Bybrawe loop plugin will register this job for the current session. If the action is a prompt, keep working autonomously and do not wait for the user between iterations.
